<%= form_for order do |f| %>
		
		 <%= f.hidden_field :product_id %>
		 <%= f.hidden_field :user_id %>

		 <p>
 			<%= f.text_field :date, :class => "form-control" %>
		 </p>
	
		 <%= f.submit "Attend now!", :class => "btn-lg btn-success" %>

<% end %>
